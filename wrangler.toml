name = "open-gateway"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "docs"

# Build configuration (if using a static site generator)
# [build]
# command = "echo 'No build needed for static docs'"
# cwd = ""

[env.production]
name = "open-gateway"

[env.preview]
name = "open-gateway-preview"
